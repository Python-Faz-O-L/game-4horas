import pygame
pygame.init()

tamanhoTela = (800,400)

screen = pygame.display.set_mode(tamanhoTela)

running = True
while running:
  # analisar eventos
  if event.type = pygame.QUIT:
  
  # desenhar elementos
  # atualizar tudo
  pygame.display.update()

pygame.quit()
